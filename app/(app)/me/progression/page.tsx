import { ProgressionScreen } from "@/components/progression/ProgressionScreen";

// Viewer Progression. The same screen coaches use, with an in-toolbar swimmer
// picker scoped server-side to the viewer's linked swimmer(s).
export default function ViewerProgressionPage() {
  return <ProgressionScreen />;
}
