import { ViewerSwimmersScreen } from "@/components/me/ViewerSwimmersScreen";

// The viewer's My swimmers list — the swimmer(s) they have been granted access
// to, each linking to a read-only profile. The viewer's home route.
export default function ViewerSwimmersPage() {
  return <ViewerSwimmersScreen />;
}
