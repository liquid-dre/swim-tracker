import { StandardsScreen } from "@/components/standards/StandardsScreen";

// Viewer Standards. The same qualifying-standards reference coaches see,
// read-only (only the super-user can edit cuts).
export default function ViewerStandardsPage() {
  return <StandardsScreen />;
}
