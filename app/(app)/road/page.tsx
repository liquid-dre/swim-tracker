import { RoadScreen } from "@/components/road/RoadScreen";

// Road to qualify (Step 12, BRD §5.10–5.11). Per-swimmer gap-to-cut + qualifying
// progress; opens on the all-tiers view with a per-session tier toggle. Coach-
// facing, LCM only.
export default function RoadPage() {
  return <RoadScreen />;
}
