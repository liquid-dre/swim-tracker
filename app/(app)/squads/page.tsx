import { ComingSoon } from "@/components/shell/ComingSoon";

export default function SquadsPage() {
  return (
    <ComingSoon
      href="/squads"
      description="Group swimmers into squads so you can compare and track them together."
    />
  );
}
